---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: rocketaihubs.operator.ibm.com
spec:
  group: operator.ibm.com
  names:
    kind: RocketAIHub
    listKind: RocketAIHubList
    plural: rocketaihubs
    singular: rocketaihub
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RocketAIHub is the Schema for the rocketaihubs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RocketAIHubSpec defines the desired state of RocketAIHub
            properties:
              components:
                description: Components defines the components to be installed
                properties:
                  kServe:
                    properties:
                      install:
                        default: true
                        type: boolean
                    type: object
                  katib:
                    properties:
                      install:
                        default: true
                        type: boolean
                    type: object
                  kubeflowNotebooks:
                    properties:
                      install:
                        default: true
                        type: boolean
                    type: object
                  kubeflowPipelines:
                    properties:
                      install:
                        default: true
                        type: boolean
                    type: object
                  trainingOperator:
                    properties:
                      install:
                        default: true
                        type: boolean
                    type: object
                type: object
            type: object
          status:
            description: RocketAIHubStatus defines the observed state of RocketAIHub
            properties:
              condition:
                type: string
              message:
                type: string
            required:
            - condition
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
